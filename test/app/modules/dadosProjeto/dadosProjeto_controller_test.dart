import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:orcamento_mestre/app/modules/dadosProjeto/projeto_controller.dart';
import 'package:orcamento_mestre/app/modules/dadosProjeto/dadosProjeto_module.dart';

void main() {
  initModule(DadosProjetoModule());
  // DadosProjetoController dadosprojeto;
  //
  setUp(() {
    //     dadosprojeto = DadosProjetoModule.to.get<DadosProjetoController>();
  });

  group('DadosProjetoController Test', () {
    //   test("First Test", () {
    //     expect(dadosprojeto, isInstanceOf<DadosProjetoController>());
    //   });

    //   test("Set Value", () {
    //     expect(dadosprojeto.value, equals(0));
    //     dadosprojeto.increment();
    //     expect(dadosprojeto.value, equals(1));
    //   });
  });
}
