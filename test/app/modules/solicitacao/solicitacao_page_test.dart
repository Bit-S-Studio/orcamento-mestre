import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:orcamento_mestre/app/modules/solicitacao/solicitacao_page.dart';

void main() {
  testWidgets('SolicitacaoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(SolicitacaoPage(title: 'Solicitacao')));
    //  final titleFinder = find.text('Solicitacao');
    //  expect(titleFinder, findsOneWidget);
  });
}
