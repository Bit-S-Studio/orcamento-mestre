import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:orcamento_mestre/app/app_module.dart';

void main() {
  initModule(AppModule());
  // BudgetController budget;
  //
  setUp(() {
    //     budget = AppModule.to.get<BudgetController>();
  });

  group('BudgetController Test', () {
    //   test("First Test", () {
    //     expect(budget, isInstanceOf<BudgetController>());
    //   });

    //   test("Set Value", () {
    //     expect(budget.value, equals(0));
    //     budget.increment();
    //     expect(budget.value, equals(1));
    //   });
  });
}
