import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:orcamento_mestre/app/app_module.dart';

void main() {
  initModule(AppModule());
  // BaseController base;
  //
  setUp(() {
    //     base = AppModule.to.get<BaseController>();
  });

  group('BaseController Test', () {
    //   test("First Test", () {
    //     expect(base, isInstanceOf<BaseController>());
    //   });

    //   test("Set Value", () {
    //     expect(base.value, equals(0));
    //     base.increment();
    //     expect(base.value, equals(1));
    //   });
  });
}
