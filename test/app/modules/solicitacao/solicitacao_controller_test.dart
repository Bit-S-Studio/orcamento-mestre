import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:orcamento_mestre/app/modules/solicitacao/solicitacao_controller.dart';
import 'package:orcamento_mestre/app/modules/solicitacao/solicitacao_module.dart';

void main() {
  initModule(SolicitacaoModule());
  // SolicitacaoController solicitacao;
  //
  setUp(() {
    //     solicitacao = SolicitacaoModule.to.get<SolicitacaoController>();
  });

  group('SolicitacaoController Test', () {
    //   test("First Test", () {
    //     expect(solicitacao, isInstanceOf<SolicitacaoController>());
    //   });

    //   test("Set Value", () {
    //     expect(solicitacao.value, equals(0));
    //     solicitacao.increment();
    //     expect(solicitacao.value, equals(1));
    //   });
  });
}
