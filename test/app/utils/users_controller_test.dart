import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:orcamento_mestre/app/app_module.dart';

void main() {
  initModule(AppModule());
  // UsersController users;
  //
  setUp(() {
    //     users = AppModule.to.get<UsersController>();
  });

  group('UsersController Test', () {
    //   test("First Test", () {
    //     expect(users, isInstanceOf<UsersController>());
    //   });

    //   test("Set Value", () {
    //     expect(users.value, equals(0));
    //     users.increment();
    //     expect(users.value, equals(1));
    //   });
  });
}
