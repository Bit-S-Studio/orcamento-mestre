import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:orcamento_mestre/app/modules/budget/budget_page.dart';

void main() {
  testWidgets('BudgetPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(BudgetPage(title: 'Budget')));
    //  final titleFinder = find.text('Budget');
    //  expect(titleFinder, findsOneWidget);
  });
}
