import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:orcamento_mestre/app/modules/tipo/tipo_controller.dart';
import 'package:orcamento_mestre/app/app_module.dart';

void main() {
  initModule(AppModule());
  // TipoController tipo;
  //
  setUp(() {
    //     tipo = AppModule.to.get<TipoController>();
  });

  group('TipoController Test', () {
    //   test("First Test", () {
    //     expect(tipo, isInstanceOf<TipoController>());
    //   });

    //   test("Set Value", () {
    //     expect(tipo.value, equals(0));
    //     tipo.increment();
    //     expect(tipo.value, equals(1));
    //   });
  });
}
