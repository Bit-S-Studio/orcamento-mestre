import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:orcamento_mestre/app/modules/dadosProjeto/dadosProjeto_page.dart';

void main() {
  testWidgets('DadosProjetoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DadosProjetoPage(title: 'DadosProjeto')));
    //  final titleFinder = find.text('DadosProjeto');
    //  expect(titleFinder, findsOneWidget);
  });
}
