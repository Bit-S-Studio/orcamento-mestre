import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('DadosEmpresaPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DadosEmpresaPage(title: 'DadosEmpresa')));
    //  final titleFinder = find.text('DadosEmpresa');
    //  expect(titleFinder, findsOneWidget);
  });
}
