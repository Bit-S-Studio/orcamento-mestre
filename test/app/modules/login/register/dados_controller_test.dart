import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:orcamento_mestre/app/utils/dados_controller.dart';
import 'package:orcamento_mestre/app/modules/login/register/register_module.dart';

void main() {
  initModule(RegisterModule());
  // DadosController dados;
  //
  setUp(() {
    //     dados = RegisterModule.to.get<DadosController>();
  });

  group('DadosController Test', () {
    //   test("First Test", () {
    //     expect(dados, isInstanceOf<DadosController>());
    //   });

    //   test("Set Value", () {
    //     expect(dados.value, equals(0));
    //     dados.increment();
    //     expect(dados.value, equals(1));
    //   });
  });
}
