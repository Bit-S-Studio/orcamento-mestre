import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:orcamento_mestre/app/modules/tipo/tipo_page.dart';

void main() {
  testWidgets('TipoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TipoPage(title: 'Tipo')));
    //  final titleFinder = find.text('Tipo');
    //  expect(titleFinder, findsOneWidget);
  });
}
