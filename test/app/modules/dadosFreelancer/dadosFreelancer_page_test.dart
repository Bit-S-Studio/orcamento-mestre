import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('DadosFreelancerPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DadosFreelancerPage(title: 'DadosFreelancer')));
    //  final titleFinder = find.text('DadosFreelancer');
    //  expect(titleFinder, findsOneWidget);
  });
}
